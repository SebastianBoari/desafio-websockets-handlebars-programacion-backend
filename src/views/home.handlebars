<div>
    <h1>Productos</h1>
    {{#each products}}
        <div>
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
        </div>
    {{/each}}
</div>